package main;
import javax.swing.*;
import java.awt.*;

public class Board {
    public Board() {
        JFrame board = new JFrame();

        board.setSize(600, 1000);
        board.getContentPane().setBackground(Color.BLACK);
        board.setVisible(true);
    }
}
